;(function(exports){

	var Light = function(options) {

		this.x = options.x;
		this.y = options.y;
		this.color = options.color;

	};

	Light.prototype.loop(test) {

	}

	var getIntersection = function(ray, segment) {
		var ray  = {
			px = ray.
		}
	}

	exports.Light = Light;
})(this);
